# Phase 07: 搜索、验收与打包

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 全文搜索可用；按 H01–H14 手工验收；release 构建并确认安装包体积与基本性能门槛。

**Architecture:** `search_files` 后端遍历；前端简单搜索条/面板；`Cargo.toml` release profile 按规格压体积。

**Tech Stack:** 已有 fs_ops · Vitest/cargo test · `pnpm tauri build`

## Global Constraints

见 [`00-落地总览.md`](./00-落地总览.md)。依赖阶段 01–06。

---

### Task 1: `search_files`

**Files:**
- Modify: `src-tauri/src/commands/fs_ops.rs`
- Create: `src/components/SearchPanel.vue`（或工具栏搜索框 + 结果列表）
- Modify: `src/api/tauri.ts`、`AppToolbar.vue`

**Interfaces:**

```rust
#[derive(Serialize)]
#[serde(rename_all = "camelCase")]
pub struct SearchHit {
    pub path: String,
    pub line: u32,
    pub preview: String, // ≤120 chars
}

#[tauri::command]
pub fn search_files(root: String, keyword: String) -> AppResult<Vec<SearchHit>>
// 大小写不敏感；最多 200 条；跳过点目录；只搜 md/markdown/txt
```

- [ ] **Step 1: tempfile 测试 — 大小写、上限 200、预览截断**

- [ ] **Step 2: 实现 command + 前端结果列表**

- [ ] **Step 3: 点击结果 → `selectFile` + `editor.load`（行号滚动 v1.0 尽力，可仅打开文件）**

- [ ] **Step 4: 手工 H07**

---

### Task 2: 系统 Git 缺失提示（收口）

**Files:**
- Modify: `git_ops.rs` push/pull/clone 入口
- Modify: 前端错误展示

**Interfaces:**
- 启动时或首次推拉时检测 `git --version`；失败 Toast：`请安装 Git 并确保在 PATH 中`

- [ ] **Step 1: 实现检测与文案**

---

### Task 3: Release 体积优化

**Files:**
- Modify: `src-tauri/Cargo.toml`

**Interfaces:**

```toml
[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
```

- [ ] **Step 1: 写入 profile**

- [ ] **Step 2: 构建**

```bash
pnpm tauri build
```

- [ ] **Step 3: 测量产物**

Expected: Windows 安装包（msi/nsis）**< 15MB**（目标约 8–10MB）。记录实际大小到本地笔记（不强制改规格）。

---

### Task 4: 手工验收清单（发布门禁）

对照规格 §12.3，在 Windows 上全部勾选：

| ID | 项 | 通过 |
|----|-----|------|
| H01 | 打开工作区 | [ ] |
| H02 | 新建文件/目录 | [ ] |
| H03 | 800ms 自动保存 | [ ] |
| H04 | Ctrl+S | [ ] |
| H05 | 切换文件先存后载 | [ ] |
| H06 | 预览 GFM/高亮 | [ ] |
| H07 | 全文搜索 | [ ] |
| H08 | Git init/commit | [ ] |
| H09 | push/pull 通知与冲突 | [ ] |
| H10 | Token 地址复制与克隆推拉 | [ ] |
| H11 | 托盘与快捷键 | [ ] |
| H12 | 单实例 | [ ] |
| H13 | 主题 | [ ] |
| H14 | 窗口状态 | [ ] |

附加：

- [ ] 空闲内存观感 < 200MB（任务管理器）
- [ ] 无 `allow-all`；CSP 仍在
- [ ] `cargo test` 与 `vitest`（utils）PASS

---

### Task 5: 图标与用户说明（最小）

**Files:**
- 准备 1024×1024 PNG → `pnpm tauri icon <path>`
- 可选本地：`doc/用户快速开始.md`（如何创建 Gitee Token、如何复制带 Token 地址）— 仅本地参考

- [ ] **Step 1: 生成多尺寸图标**

- [ ] **Step 2: 再打一次 build 确认图标生效**

---

## 阶段验收（v1.0 完成定义）

- [ ] H01–H14 全过
- [ ] 安装包 < 15MB
- [ ] 带 Token 地址外部 git 与应用内推拉均可用
- [ ] 规格 §1.4 / §13.2 未做项均未误做

**返回：** [00-落地总览.md](./00-落地总览.md)
